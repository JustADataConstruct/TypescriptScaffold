const { env } = process;

export const config = {
    // Bring your .env keys here so you can get autocompletion.
    // You must load the .env file first calling node --env-file=.env on your run script.
    // export const config {
    //    test_key:env.test_key,
    //}
    // index.ts:
    //import { config } from "./config";
    //const test = config.test_key;
}
